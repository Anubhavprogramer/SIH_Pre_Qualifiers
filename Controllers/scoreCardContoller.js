const asyncHandler = require("../middlewares/asyncHandler");

const CreateScoreCard = asyncHandler((req, res) => {
    // add a code to create a score card
});

const GetCard = asyncHandler((req, res)=>{
    // add a code to get a score card
})

module.exports ={
    CreateScoreCard,
    GetCard
}